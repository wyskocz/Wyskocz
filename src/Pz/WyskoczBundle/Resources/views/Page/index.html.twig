{% extends '::admin.html.twig' %}
{% block title %}
Lista stron
{% endblock %}
{% block body -%}
<h3>Lista stron</h3>
    <table class="table">
        <thead>
            <tr>
                <th>#</th>
                <th>Tag</th>
                <th>Title</th>
                <th>Content</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_page_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tag }}</td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.content }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_page_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_page_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('admin_page_new') }}">
        <button type="button" class="btn btn-primary btn-sm">Dodaj stronę</button>
    </a>
    {% endblock %}
